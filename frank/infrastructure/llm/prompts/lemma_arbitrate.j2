Pick one lemma for each type. Prefer a dictionary citation form.
Candidates are the two analyzer lemmas; do not invent unrelated words.
Return JSON with an `items` array of `{surface, upos, lemma}`.

{{ batch_json }}
