Pick one reunited lemma, or null if the particle does not belong with the verb.
German: ruft + an → anrufen. Hungarian: el + olvas → elolvas, never el + tud.
Return JSON with an `items` array of `{particle, verb, reunited_lemma}`.

{{ batch_json }}
