One Ukrainian rendering and a short note per term. Do not localize names unless
the lemma is a conventional exonym already applied outside this call.
German names: ei→ай, eu→ой, sch→ш, z→ц, w→в, ü→ю, drop h after a vowel
(Heinrich→Гайнріх, Hans→Ганс). Hungarian: s→ш, sz→с, cs→ч, zs→ж, gy→дь,
á→а, é→е, ö/ő→е, ü/ű→ю; family-first source order becomes given-name-first
in Ukrainian (Kovács János→Янош Ковач). Idioms: Ukrainian equivalent, note the
literal image. DISAMBIG: one gloss, not a definition dump.
Return JSON with an `items` array of `{lemma, translation_uk, note}`.
Source language: {{ lang }}

{{ batch_json }}
